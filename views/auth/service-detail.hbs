{{!-- <h1 class="mb-5"> Hello {{ user.username }} !!</h1> --}}

<div class="detail-card card">
  <div class="row mb-3">
    <div class="col-4">
      <div class="profile-img" style="background-image: url({{ createdUser.picture }});"></div>
      {{!-- <img class="img-fluid" src="{{ createdUser.picture }}"> --}}
    </div>
    <div class="col-8">
      <h3>{{ serviceDetail.title }} <small>by {{ createdUser.username }}</small></h3>
      <p>{{ serviceDetail.category }}</p>
      <p>{{ serviceDetail.time }}</p>
      <p>{{ serviceDetail.location }}</p>
    </div>
  </div>
</div>
<p>{{ serviceDetail.description }}</p>



<a  href="/auth/requested/{{ serviceDetail._id }}" class="btn btn-primary mb-3">Request</a>
